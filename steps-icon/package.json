{
  "name": "@umi-block/steps-icon",
  "version": "1.0.0",
  "description": "通过设置 `Steps.Step` 的 `icon` 属性，可以启用自定义图标。",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
