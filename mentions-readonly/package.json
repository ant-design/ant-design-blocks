{
  "name": "@umi-block/mentions-readonly",
  "version": "1.0.0",
  "description": "通过 `disabled` 属性设置是否生效。通过 `readOnly` 属性设置是否只读。",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
